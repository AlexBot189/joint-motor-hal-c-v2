# 外骨骼外设节点系统框架设计

> 版本: V1.0  
> 日期: 2026-08-01  
> 作者: zhiqiang.yang  
> 项目: KWS EXOSKELETON V2  
> 目标: 记录 stark_periph_manager_node 整体架构，供内部技术分享和新人交接

---

## 1. 系统定位

`stark_periph_manager_node` 是外骨骼机器人 RV1126B 平台上的外设管理进程，负责：

- CAN FD 总线上的关节电机通信（PDO 实时控制 + SDO 参数配置）
- 传感器数据聚合（IMU + 霍尔 + 力矩 + 气压计）
- 与算法进程的实时数据交换（共享内存）
- 安全监控与故障保护
- 物理按键交互和状态指示

节点位于算法进程和电机驱动板之间的中间层，屏蔽硬件细节，提供标准化的控制和数据接口。

```
┌─────────────────────────────────────────────────────────┐
│                 算法进程 (外骨骼步态控制)                  │
│  - 读 SHM: 双电机位置/速度/电流 + IMU 姿态 + 传感器数据      │
│  - 写 SHM: 控制命令 (扭矩/位置/速度/MIT) + 心跳            │
│  - 通过 stark_client.h API 访问, 无 HAL 依赖         │
└───────────────────────┬─────────────────────────────────┘
                        │ /dev/shm/stark_shm (64KB 共享内存)
════════════════════════╪════════════════════════════════════
                        │
┌───────────────────────┴─────────────────────────────────┐
│            stark_periph_manager_node (本节点)             │
│                                                          │
│  ┌──────────────────────────────────────────────────┐   │
│  │           CanDispatcher (观察者模式核心)           │   │
│  │   InitDispatcher() → CANFD + 电机注册 + SHM 创建   │   │
│  └──────────────────────┬───────────────────────────┘   │
│                         │                                │
│  ┌──────────────────────┼───────────────────────────┐   │
│  │         线程模型 (6 个线程)                        │   │
│  │  [RT] 控制线程     1KHz prio=90  mailbox→PDO      │   │
│  │  [RT] CAN接收线程   事件 prio=85  帧分发→缓存       │   │
│  │  [RT] RT工作线程    1KHz prio=90  控制+上报+安全    │   │
│  │  [非RT] 状态机线程   50ms prio=0  启动/校准/故障     │   │
│  │  [非RT] SDO遥测线程  0.5ms prio=0  位置/温度轮询      │   │
│  │  [非RT] 日志drain    50ms prio=0  ringbuf→spdlog   │   │
│  └──────────────────────────────────────────────────┘   │
│                         │                                │
│  ┌──────────────────────┼───────────────────────────┐   │
│  │         可选模块                                   │   │
│  │  RosAdapter    → ROS Topic/Service (调试)         │   │
│  │  WebServer     → WebSocket 实时调试面板            │   │
│  │  ButtonHandler → GPIO 按键 (校准/上报切换)         │   │
│  └──────────────────────────────────────────────────┘   │
└───────────────────────┬─────────────────────────────────┘
                        │ CAN FD (1M arbitration / 5M data)
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
   ┌─────────┐    ┌─────────┐    ┌─────────┐
   │ 右髋电机 │    │ 左髋电机 │    │  IMU    │
   │ ID=1    │    │ ID=2    │    │ICM45608 │
   │ CAN FD  │    │ CAN FD  │    │     I2C │
   └─────────┘    └─────────┘    └─────────┘
```

---

## 2. 核心设计原则

### 2.1 观察者模式解耦

继承自 petrobot_periph_manager 的 Observer 模式，CanDispatcher 实现 `IMsgInternalDispatcher` 接口：

```
CanDispatcher (CAN协议解析)
    │
    ├── NotifyObserver(type, data)
    │       │
    │       ├──→ RosAdapter  (type=ROS)   → ROS Topic/Service
    │       └──→ WebAdapter  (type=WEB)   → WebSocket Push
    │
    └── 直接 SHM 写入 (反馈路径, 不走Observer)
            fb_buffer → 算法进程读取
```

- Dispatcher 只负责协议解析和数据分发，不处理业务逻辑
- Observer 各自独立，新增 Observer 不影响现有逻辑
- 使用 `boost::any` 传递数据，运行时类型判断

### 2.2 RT 与非 RT 分离

- **RT 路径**: 控制命令下发 (PDO)、反馈数据收集、安全监控
  - 禁止阻塞调用 (无 SDO、无 malloc、无 sleep)
  - 只用无锁数据结构 (SHM atomic、ring buffer)
  - 绑定 CPU 核心，SCHED_FIFO 调度

- **非 RT 路径**: 电机启动 (SDO)、校准 (SDO)、状态机、ROS、Web
  - 允许阻塞和 SDO 通信
  - SCHED_OTHER 调度

### 2.3 共享内存单写多读

- 反馈数据: motor_node 写，算法/Web 读，双 Buffer 防撕裂
- 控制命令: 算法写，motor_node 读，环形缓冲 SPSC 无锁
- 状态数据: motor_node 写，算法只读，atomic 保证可见性

### 2.4 故障隔离

- 电机 HAL 层内部异常不会传播到应用层
- CAN 断线重连由 HAL 自动处理
- 校准超时有明确的超时退出机制
- SHM 操作失败不影响进程存活

---

## 3. 启动流程

```
main()
  ├─ 1. 注册信号处理 (SIGINT/SIGTERM/SIGPIPE)
  ├─ 2. 解析命令行参数 (-c config_path)
  │
  ├─ 3. CanDispatcher::InitDispatcher()
  │     ├─ 3.1 LoadMotorConfig()          // 解析 config.json
  │     ├─ 3.2 motor_hal_init("can0")     // 打开 CAN FD
  │     ├─ 3.3 motor_hal_add_motor() ×2   // 注册电机
  │     ├─ 3.4 motor_hal_recv_set_rt()    // 接收线程 RT 配置
  │     ├─ 3.5 motor_hal_recv_start()     // 启动 CAN 接收
  │     ├─ 3.6 new StarkMotorCtrl(hal)    // 控制封装
  │     ├─ 3.7 ImuHALSensor::Init()       // IMU 初始化
  │     └─ 3.8 stark_shm_mgr_open()       // 创建 SHM
  │
  ├─ 4. state_transition(STATE_BOOTING)    // 进入启动状态
  │
  ├─ 5. new StarkRtWorker()               // 创建 RT 线程 (先不启动)
  │
  ├─ 6. 启动日志 drain 线程
  │
  ├─ 7. 注入全局上下文 g_ctx
  │     ├─ hal, shm, motor_count
  │     ├─ sensor 配置 (period_div, bus_format, mode, force_module)
  │     └─ 校准/上报/LED/按键配置
  │
  ├─ 8. 可选初始化
  │     ├─ RosAdapter (ENABLE_ROS)
  │     └─ WebServer  (ENABLE_WEBSERVER)
  │
  ├─ 9. main_loop_run() — 主循环入口
  │     ┌─────────────────────────────────────────┐
  │     │ while (g_running) {                    │
  │     │   ros::spinOnce();                     │
  │     │   poll_common();    // auto-startup    │
  │     │   switch (state) {                     │
  │     │     BOOTING → poll_booting()           │
  │     │     READY   → poll_ready()             │
  │     │     RUNNING → (按键/校准处理)           │
  │     │   }                                    │
  │     │   poll_rt_pending();                   │
  │     │   poll_sdo_commands();                 │
  │     │   g_btn_handler->poll();               │
  │     │   usleep(50000);  // 50ms 轮询         │
  │     │ }                                      │
  │     └─────────────────────────────────────────┘
  │
  └─ 10. 清理 (sync_stop → RT stop → calib destroy → dispatcher destroy)
```

---

## 4. 状态机

```
        ┌──────────┐
        │ BOOTING  │  CANFD 初始化 + 等待电机上线
        └────┬─────┘
             │ 电机全在线 (或 5s 超时降级)
             ▼
        ┌──────────┐
   ┌───→│  READY   │  等待校准触发 (按键/首次启动跳过)
   │    └────┬─────┘
   │         │ 校准完成 / 首次启动跳过
   │         ▼
   │    ┌──────────┐
   │    │ RUNNING  │  RT 线程全速运行，周期上报
   │    └────┬─────┘
   │         │ 校准请求 (按键 3 连击)
   │         │ (停 RT → 回 READY → 执行校准 → 完成回 RUNNING)
   │         │ 取消请求 (按键 3 连击)
   │         │ (停 RT → 关透传 → 失能 → 回 READY)
   └─────────┘
        ┌──────────┐
        │  FAULT   │  异常状态 (当前未使用，预留)
        └──────────┘
```

**状态转换规则**：
- BOOTING → READY: 所有已注册电机在线 (或 5s 超时)
- READY → RUNNING: 首次启动跳过校准，或按键触发校准完成后
- RUNNING → READY: 按键触发校准 (CALIBRATE 或 CANCEL)
- 任意状态 → FAULT: 预留

---

## 5. 数据流

### 5.1 反馈数据流 (电机 → 算法, 200Hz)

```
CAN FD 帧到达
  │
  ├─ 0x300 (反馈帧) ──→ fb_cache[motor_id]
  │                       motor_feedback_t:
  │                         position, velocity, current_iq,
  │                         error_code, temperature, torque_nm,
  │                         mode_fb, status_byte
  │
  ├─ 0x680 (传感器透传) → sensor_cache[motor_id]
  │                         hall_adc0/1/2, force_raw, knee_hall,
  │                         key_landing, data_valid
  │
  ├─ 0x6B0 (SPI力矩)   → sensor_cache[motor_id]
  │                         spi_force_raw_s24, spi_valid, spi_error
  │
  └─ IMU (SPI/I2C)     → imu_data_t (9轴融合)
       │
       ▼
  RT 上报线程 (200Hz):
    ├─ motor_hal_get_feedback()  → 读 fb_cache
    ├─ motor_hal_get_sensor()    → 读 sensor_cache
    ├─ ImuHALSensor::Read()      → 读 IMU 融合数据
    │
    ├─ 组装 feedback_frame_t → 写 fb_buffer[active^1]
    ├─ 内存屏障 (wmb)
    ├─ active_idx = active^1  (原子切换)
    └─ 算法进程检测到 active_idx 变化 → 读 fb_buffer[active]
```

### 5.2 控制命令流 (算法 → 电机, 1KHz)

```
算法进程:
  stark_torque/multi/pp/pv/mit()  // stark_client.h inline API
    │
    ├─ _stark_mbox_begin()        // SPSC 环形缓冲 slot 分配
    ├─ 写入 mailbox.frames[slot].cmd[id-1]
    └─ _stark_mbox_commit()       // seq_write 原子递增
        │
        ▼ RT 控制线程 (1KHz):
  ProcessMailbox():
    r = seq_read, w = seq_write
    for slot in [r..w):
      cmd = mailbox.frames[slot]
      ├─ STARK_CMD_TORQUE  → motor_hal_multi_ctrl(mode=CURRENT)
      ├─ STARK_CMD_POS     → motor_hal_multi_ctrl(mode=CSP)
      ├─ STARK_CMD_MULTI   → 双电机一帧广播
      ├─ STARK_CMD_MIT     → 走 0x110 MIT 独立帧
      └─ STARK_CMD_SET_MODE → 切换 PDO 模式 (双发防丢帧)
    seq_read += count
```

### 5.3 管理命令流 (算法 → 电机, 独立通道)

```
算法进程:
  stark_enable/disbale/estop/recover/clear_fault(1)
    │
    ├─ 写入 shm->mgmt_cmd[0]
    └─ mgmt_seq[0] 原子递增
        │
        ▼ RT 工作线程 (每周期检查):
  ProcessMgmt():
    for i in [0..motor_count):
      if mgmt_seq[i] != mgmt_ack[i]:
        ├─ STARK_CMD_ENABLE → PDO enable
        ├─ STARK_CMD_DISABLE → PDO disable + 电流清零
        ├─ STARK_CMD_ESTOP  → PDO estop + 刹车抱死
        └─ ...
        mgmt_ack[i] = mgmt_seq[i]
```

---

## 6. 目录结构

```
stark_periph_node/
├── CMakeLists.txt                 # 构建配置
├── setup_rt_env.sh               # RT 环境初始化脚本
└── src/
    ├── main.cpp                   # 入口: 初始化 → 主循环 → 清理
    ├── stark_shm.h                # SHM 布局定义 (算法侧也引用)
    ├── stark_client.h             # 算法侧纯 C API (header-only)
    │
    ├── main_loop/
    │   ├── main_loop.h            # 主循环外部接口
    │   └── main_loop.cpp          # 状态分发 + 校准逻辑 + 按键/LED/SDO
    │
    ├── motor/
    │   ├── motor_init.h/cpp       # CanDispatcher: CANFD + motor注册 + SHM
    │   ├── motor_state.h/cpp      # 状态机: 4状态 enter/exit 钩子
    │   ├── motor_rt_worker.h/cpp  # RT工作线程: 控制消费 + 上报 + 安全
    │   ├── motor_ctrl.h/cpp       # StarkMotorCtrl: SDO 控制封装
    │   └── motor_context.h        # 全局上下文 (g_ctx)
    │
    ├── imu/
    │   ├── imu_sensor.h           # IMU HAL 封装接口
    │   └── imu_sensor.cpp         # ICM45608 9轴融合数据读写
    │
    ├── shm/
    │   ├── shm_mgr.h              # SHM 创建/销毁/映射管理
    │   └── shm_mgr.cpp
    │
    ├── interface/
    │   ├── Defines.hpp            # 公共宏和常量
    │   ├── IListener.hpp           # Observer 接口
    │   └── IMsgInternalDispatcher.hpp  # 调度器接口
    │
    ├── utils/
    │   ├── factory.h/cpp          # Observer 工厂
    │   ├── latency_trace.h/cpp    # 端到端延迟追踪
    │   └── rt_log.h               # RT 线程安全日志 ringbuf
    │
    ├── ros/
    │   ├── RosAdapter.h/cpp       # ROS Topic/Service 适配器
    │   └── RosTopic.hpp           # ROS Topic 枚举
    │
    ├── web/
    │   ├── WebServer.h/cpp        # Crow WebSocket 调试服务器
    │   └── stark_node_debug.html  # 调试面板
    │
    ├── button/
    │   ├── GPIOMonitor.h/cpp      # GPIO 边沿监测
    │   └── button_handler.h/cpp   # 按键多击检测
    │
    ├── log_helper/                # spdlog 异步日志封装
    ├── config/                    # 配置文件
    │   ├── stark_config.json      # 双电机配置
    │   └── stark_config_single.json  # 单电机配置
    ├── doc/                       # 文档
    └── test/
        └── demo_algo.c            # 算法控制示例
```

---

## 7. 配置模型

配置文件 `stark_config.json`:

```json
{
  "can": {                           // CAN FD 硬件配置
    "interface": "can0",
    "arbitration_rate": 1000000,     // 仲裁段 1Mbps
    "data_rate": 5000000             // 数据段 5Mbps
  },
  "motors": [{                       // 电机配置 (每电机独立)
    "id": 1,
    "name": "right_hip",
    "heartbeat_ms": 0,              // 0=关闭心跳看门狗
    "profile_accel": 500,           // 默认加减速 RPM/s
    "profile_decel": 500,
    "profile_velocity": 20,         // 默认轮廓速度 RPM
    "disable_watchdog": true,       // 关闭驱动板看门狗
    "auto_enable": false,           // 不自动使能
    "tpdo_sync_count": 1            // SYNC 触发 TPDO 计数
  }],
  "shm": {                           // 共享内存
    "name": "/stark_shm",
    "size_kb": 64
  },
  "rt": {                            // 实时线程配置
    "enable_rt": true,
    "control_priority": 90,         // 控制线程 SCHED_FIFO 优先级
    "recv_priority": 85,            // 接收线程 SCHED_FIFO 优先级
    "control_period_us": 1000,      // 控制周期 1KHz
    "report_divider": 1,            // 上报分频 (1=每周期, 5=每5周期)
    "cpu_affinity": [3]             // CPU 亲和性
  },
  "calib": {                         // 校准
    "auto_calib": false,            // 不自动校准
    "timeout_ms": 10000,            // 超时 10s
    "threshold_deg": 1.0            // 到位阈值 1°
  },
  "imu": {                           // IMU (ICM45608)
    "i2c_dev": "/dev/i2c-3",
    "gpio_chip": "gpiochip4",
    "gpio_line": 6,
    "op_mode": 7                    // GAF 9轴融合模式
  },
  "report": {                        // 周期上报
    "auto_enable": true,            // 校准后自动开启
    "period_ms": 5                  // 上报周期
  },
  "sensor": {                        // 传感器透传
    "period_div": 4,                // 周期=4×0.25ms=1ms (V1.2基准)
    "bus_format": 3,                // CAN FD BRS
    "mode": 2,                      // 0=兼容 1=运行反馈 2=全部 3=聚合
    "force_module": 1               // 0=DF181 1=CMD_SPI
  },
  "led": { "motor_id": 2 },         // LED 控制电机
  "buttons": {                       // 物理按键
    "calib":  { "gpio_chip": "gpiochip0", "line": 5 },
    "report": { "gpio_chip": "gpiochip0", "line": 6 }
  },
  "web": {                           // Web 调试
    "enabled": false,
    "port": 8080,
    "push_period_ms": 5
  }
}
```

---

## 8. 编译与部署

### 编译

```bash
# 使用 build.sh 交叉编译
./build.sh

# 关键 cmake 选项
-DENABLE_ROS=OFF       # ROS 适配器 (调试用)
-DENABLE_WEBSERVER=ON  # Web 调试面板
```

### 依赖

| 库 | 用途 |
|----|------|
| motor_hal (C) | CAN FD 驱动 + CANopen 协议栈 |
| imu_hal (C) | ICM45608 SPI/I2C + EMD-GAF 融合 |
| log_helper | spdlog 异步日志 |
| libgpiod | GPIO 按键监测 |
| pthread + rt | Linux RT 调度 |
| crow (header-only) | WebSocket 服务器 (可选) |

### 部署

```bash
# 拷贝到设备
scp stark_periph_manager_node root@device:/data/stark/bin/
scp stark_config.json root@device:/data/config/stark/

# RT 环境初始化
./setup_rt_env.sh
# 或手动:
# echo -1 > /proc/sys/kernel/sched_rt_runtime_us
# ulimit -l unlimited

# 启动
/data/stark/bin/stark_periph_manager_node -c /data/config/stark/stark_config.json

# 查看日志
journalctl -u stark_node -f
```

---

## 9. 关键约束

1. **SHM 64KB 单页**: 所有数据必须在 64KB 内，新增字段注意 padding 对齐
2. **RT 线程零阻塞**: 控制/上报/安全线程禁止任何阻塞调用
3. **单进程**: 不支持多实例，SHM 名称 `/stark_shm` 固定
4. **CAN FD BRS 必须**: 数据段 5Mbps，不支持 Classical CAN
5. **电机 ID 固定**: 1=右髋, 2=左髋，不可运行时变更
6. **root 权限**: SHM 创建 + RT 调度 + CAN 接口都需要 root
