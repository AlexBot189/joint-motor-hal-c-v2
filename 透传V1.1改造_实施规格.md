# 透传协议 V1.1 代码改造 — 实施规格（Claw 架构 → 张三丰实现 → Claw 审查）

> 依据：《KWS 外部传感器透传协议 整合版 V1.1》
> 协议文档：/home/workspace/08-agent/openclaw/workspace/tmp/KWS外部传感器透传协议_整合版_V1.1.md
> 项目：/home/workspace/08-agent/openclaw/sw-zhangsan/business/joint-motor-hal-c-v3
> 日期：2026-07-09 ｜ 需求方：阳志强

## 阳志强的 6 条约束
1. **默认 CMD_SPI 模式**（force_module=1）
2. **周期用最快**（period_div=1 → 0.5ms / 2000Hz）
3. **尽量保持上层算法接口不变**（stark_client.h / SHM 结构 append-only，不改已有字段语义）
4. 基于 V1.1 文档改
5. 改完自审
6. **不影响其他功能**（0x300 反馈、SDO 遥测、mailbox 控制、IMU、校准等都不能受影响）

## ⚠️ 关键设计决策（务必按此，别自作主张）

### 决策1：模式必须用 mode=2（全部模式），不是 mode=1
- 现有功能依赖 **0x680** 帧携带 hall_adc0/1/2、knee_adc、hw_switch（膝关节/霍尔/着地开关）。
- mode=1（运行反馈）**不发 0x680** → 会丢掉霍尔/膝关节/开关，违反"不影响其他功能"。
- CMD_SPI 的 24 位力矩数据走 **0x6B0**。
- 所以要同时保留 0x680(hall等) + 获取 0x6B0(CMD_SPI力矩) → **必须 mode=2（全部模式）**，四帧全发。
- 副作用：0x690/0x6A0 也会收到（位置/速度/Iq/温度），先**缓存+提供getter**，不接入现有 0x300/SDO 路径（避免影响现有功能）。

### 决策2：CMD_SPI 模式下 0x680 的 force_raw/data_valid 固定为 0
- 协议明确：CMD_SPI 模式，0x680 的 force_raw 和 data_valid 固定 0，力矩完整数据只在 0x6B0。
- 所以现有 `_parse_sensor_frame` 对 force_raw/data_valid 的解析在 CMD_SPI 模式下会拿到 0，属正常。**不要**用 0x680 的 force_raw 当 CMD_SPI 力矩。

### 决策3：数据结构一律 append-only（保护上层接口）
- `motor_sensor_t`：保留现有字段，**追加** force_ch0_s24 / force_ch1_s16 / force_valid / force_error。
- `PeriodicUploadData` / `stark_sensor_data_t`：**只在尾部追加**新字段，不改动/不重排现有字段。df181_torque(uint16) 保留原义（DF181模式用）。
- 现有 `data_valid` 语义不变（DF181 CH0 有效标志）。

### 决策4：周期换算基准变更 250us→500us
- 旧代码 main_loop.cpp:140 `period_div = sensor_period_ms * 4`（基于 250us tick）。
- 新协议基准 **0.5ms**：period_div = 周期ms / 0.5 = ms×2。
- 但需求要"最快周期"=period_div=1(0.5ms)，整数 ms 表达不了 0.5ms。
- **改法**：config 改为直接配 `period_div`（默认 1），不再用 period_ms×倍数。保留 period_ms 兼容旧 config：若配了 period_div 用之，否则 period_div = period_ms×2。

---

## 逐文件改动清单

### F1. motor_hal/inc/motor_hal_types.h
```c
/* 新增 COB-ID */
#define COB_RUNFB0_BASE   (0x690)   /* 运行反馈0: 位置/速度 */
#define COB_RUNFB1_BASE   (0x6A0)   /* 运行反馈1: Iq/母线电流/温度/错误码 */
#define COB_FORCE_BASE    (0x6B0)   /* 力/力矩反馈: DF181双S16 或 CMD_SPI单S24 */

/* 透传模式 / 力矩模块 枚举 */
typedef enum { SENSOR_MODE_COMPAT=0, SENSOR_MODE_RUNFB=1, SENSOR_MODE_ALL=2 } sensor_tx_mode_t;
typedef enum { FORCE_MODULE_DF181=0, FORCE_MODULE_CMD_SPI=1 } force_module_t;

/* motor_sensor_t: 尾部追加(现有字段不动) */
    /* --- V1.1 追加: 0x6B0 力矩帧 --- */
    int32_t  force_ch0_s24;   /* CMD_SPI: 24位有符号(已符号扩展); DF181: CH0 S16 提升 */
    int16_t  force_ch1_s16;   /* DF181 CH1; CMD_SPI 单通道时=0 */
    uint8_t  force_valid;     /* bit0=CH0有效 (DF181: valid_mask; CMD_SPI: valid.bit0) */
    uint8_t  force_error;     /* CMD_SPI 错误码: 0=无, 10=CRC错误; DF181=0 */
    uint8_t  force_is_cmdspi; /* 1=当前帧来自CMD_SPI(S24), 0=DF181(S16) */

/* 新增: 运行反馈缓存 (0x690/0x6A0) */
typedef struct {
    int32_t  position_actual;  /* 0x690 byte0-3, 同0x6064 */
    int32_t  velocity_actual;  /* 0x690 byte4-7, rpm, 同0x606C */
    int16_t  iq_current;       /* 0x6A0 byte0-1, 同0x6078 */
    int16_t  bus_current;      /* 0x6A0 byte2-3, mA */
    int16_t  motor_temp;       /* 0x6A0 byte4-5, 0.1°C */
    uint16_t error_code;       /* 0x6A0 byte6-7, 同0x603F */
    uint64_t timestamp_us;
} motor_runfb_t;
```

### F2. motor_hal/src/motor_hal.c
1. **CRC-8/MAXIM**（文件内 static）:
```c
static uint8_t _crc8_maxim(const uint8_t *d, int n){
    uint8_t crc=0x00;
    for(int i=0;i<n;i++){ crc^=d[i];
        for(int b=0;b<8;b++) crc=(crc&1)?(crc>>1)^0x8C:(crc>>1); }
    return crc;
}
```
2. **motor_hal_sensor_config 扩展**（加 mode + force_module）:
```c
int motor_hal_sensor_config(motor_hal_t*hal,uint8_t node_id,
        uint16_t period_div,uint8_t bus_format,uint8_t mode,uint8_t force_module){
    uint32_t cfg=(uint32_t)(period_div&0xFFFF)
               |((uint32_t)(bus_format&0x03)<<16)
               |((uint32_t)(mode&0x03)<<18)
               |((uint32_t)(force_module&0x03)<<20);
    return sdo_write_simple(hal->drv,node_id,OD_SENSOR_CONFIG,OD_SENSOR_CONFIG_SUB,cfg,4);
}
```
   - ⚠️ 6参数会破坏所有旧调用点。**兼容做法**：保留旧4参数签名为 wrapper（mode=0,force=0），新增 `motor_hal_sensor_config_ex(...6参)`。或直接改签名+更新全部调用点(见F5/F6/F7)。二选一，**推荐后者(直接改6参)**更清晰，反正调用点就几个。
3. **0x6B0 解析**（两格式，按 force_module 或按 DLC/内容判断——协议按配置的 force_module 区分；HAL 需记住当前 force_module，用 hal 或 motor_node 存一份 cur_force_module）:
```c
/* CMD_SPI: byte0-2=S24LE, byte3=0, byte4=valid, byte5=error, byte6-7=0 */
/* DF181:  byte0-1=CH0 S16, byte2-3=CH1 S16, byte4=valid_mask, byte5-7=0 */
```
   - CMD_SPI 24位符号扩展: `int32_t v=(data[0])|(data[1]<<8)|(data[2]<<16); if(v&0x800000)v|=0xFF000000;`
   - ⚠️ 注意：0x6B0 里 CMD_SPI 是 **小端**(byte0=LSB)。协议 2.6 的 CRC 计算字节序是 `data2,data1,data0`——但 2.6 的 data0/1/2 是"传感器返回顺序"(MSB first)，与 0x6B0 帧里的小端 byte 顺序相反。**实现时确认**：0x6B0.byte0..2 是否等于 raw 的 LSB..MSB。若帧内是小端(byte0=LSB=协议data2)，则 CRC 输入顺序 = 帧 byte0,byte1,byte2。**此处务必在设备上实测对齐**，别猜。→ 标记【待设备验证】
4. **0x690/0x6A0 解析** → 存 motor_runfb_t 缓存(加锁,同 sensor 缓存模式)。
5. **_dispatch_frame 增加 case** COB_RUNFB0_BASE/COB_RUNFB1_BASE/COB_FORCE_BASE。
6. **新 getter**: motor_hal_get_runfb(), motor_hal_get_force()（或并入 get_sensor 的追加字段）。
7. **motor_node 增加** cur_force_module 字段 + cached_runfb + runfb_lock。

### F3. motor_hal/inc/motor_hal.h
- 更新 sensor_config 声明(6参) + 文档；新增 get_runfb/get_force 声明。

### F4. motor_context.h + motor_init.h/.cpp + main.cpp
- context 加：`uint16_t sensor_period_div=1; uint8_t sensor_mode=2; uint8_t sensor_force_module=1;`（默认 CMD_SPI + 全部模式 + 最快）
- motor_init 解析 config：period_div(默认1)/mode(默认2)/force_module(默认1)/bus_format(默认3)；兼容旧 period_ms(×2)。
- main.cpp 把这些填进 g_node_ctx。

### F5. main_loop.cpp (poll_common, line ~140)
```c
uint16_t period_div = g_ctx->sensor_period_div;  /* 默认1=最快0.5ms */
int ret = motor_hal_sensor_config(hal, id, period_div,
              g_ctx->sensor_bus_format, g_ctx->sensor_mode, g_ctx->sensor_force_module);
```

### F6. motor_ctrl.cpp/.h  SensorConfig 包装
- SensorConfig 签名加 mode+force_module，透传给 motor_hal_sensor_config。

### F7. motor_calib.c (line 169-170) + tools/cmd_sensor.c + test/hal_stress_test.c
- 更新 sensor_config 调用为 6 参（calib 里可用 mode=0,force=0 保持原行为，或跟随全局——**calib 期间建议 mode=0/DF181 保持简单**，实测确认不影响校准）。

### F8. stark_shm.h + motor_rt_worker.cpp (Phase 2, propagate 到算法)
- PeriodicUploadData **尾部追加**（右/左各一份）：
  `int32_t force_ch0_s24; int16_t force_ch1; uint8_t force_valid; uint8_t force_err;`
- stark_sensor_data_t **尾部追加** 同上（供 feedback_frame_t 用）。
- PublishFeedback 填充这些新字段（从 motor_hal_get_force / get_sensor 追加字段取）。
- ⚠️ 结构体尺寸变了，算法侧需同步重编。df181_torque 等**现有字段全保留**。

## 不许碰（保护现有功能）
- 0x300 反馈解析 / motor_feedback_t / PublishFeedback 现有字段
- SDO 遥测线程 (0x2663/0x6064 轮询)
- mailbox 控制路径 / _pdo_send_with_switch / SafetyCheck 移除后的状态
- IMU / 气压计 / 双 Buffer 切换 / futex 唤醒
- 现有 data_valid / force_raw 语义

## 验收（对齐协议第7节）
- [ ] 编译通过（host g++ -fsyntax-only + 设备交叉编译 /opt/gcc-arm-12.4）
- [ ] mode=2 每周期收到 0x680→0x690→0x6A0→0x6B0 四帧，顺序正确
- [ ] CMD_SPI 模式 0x6B0 按 S24 解析，CRC 正确；注入 CRC 错误→ valid.bit0=0, error=10
- [ ] 0x680 的 hall/knee/switch 仍正常（不受 CMD_SPI 影响）
- [ ] period_div=1 实测周期 0.5ms
- [ ] 现有单控/连续多控/校准/上报 不回归
- [ ] 【待设备验证】0x6B0 CMD_SPI 字节序 + CRC 输入字节序对齐真实固件
