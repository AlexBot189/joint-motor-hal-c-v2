# KWS 外部传感器透传协议（整合版）

> 本文档由 V1.0（2026-06-30）与 V1.1（2026-07-08）合并整理，作为当前完整协议基线。
> 整理人：Claw ｜ 整理日期：2026-07-09
> ⚠️ 已修正 V1.1 原文 3 处疑似笔误（0x680→0x6B0），见文末"整理说明"。

---

## 文档信息

| 项目 | 内容 |
|------|------|
| 协议名称 | KWS 外骨骼系列控制器 外部传感器透传协议 |
| 当前版本 | V1.1 |
| 配置对象 | 0x5503:04 |
| 适用对象 | 主站软件、CAN/CANFD 通信集成、上位机数据解析 |

### 版本历史
| 版本 | 日期 | 说明 |
|------|------|------|
| V1.0 | 2026-06-30 | 新增运行状态反馈、双通道力/力矩反馈、发送模式选择、力/力矩传感器类型选择。旧版 0x680 接收端可继续使用。 |
| V1.1 | 2026-07-08 | CMD_SPI 原始数据由 16 位升级为**带 CRC-8/MAXIM 校验的 24 位有符号数据**，相应调整 CMD_SPI 模式下 `0x680` 与 `0x6B0` 报文内容。DF181 模式格式不变。 |

---

## 1 概述

在保留原有外部传感器帧（0x680）的基础上，新增运行状态反馈、双通道力/力矩反馈、发送模式选择以及力/力矩传感器类型选择。旧版接收端可继续使用原 `0x680 + NodeID` 报文，无需修改解析格式。

主站通过厂家自定义对象 `0x5503:04` 配置透传周期、总线格式、透传模式和力/力矩模块。

### 1.1 修改概述（相对旧版一帧透传）

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 发送模式 | 仅发送外部传感器帧 | 支持兼容 / 运行反馈 / 全部 三模式 |
| CAN 报文 | `0x680 + NodeID` 一帧 | 最多四帧，新增 `0x690`、`0x6A0`、`0x6B0` |
| 力/力矩数据 | DF181=14 位双通道；CMD_SPI=16 位单通道 | DF181 格式不变；CMD_SPI 升级为**带 CRC 校验的 24 位单通道** |
| 传感器类型 | 固件固定 | 可在 DF181 与 CMD_SPI 之间选择 |
| 发送管理 | 单帧直接发送 | 多帧按固定顺序排队发送，邮箱繁忙时延后续发 |

### 1.2 兼容性结论

- 原有 `0x680 + NodeID` 的 8 字节 bit 位定义**保持不变**；DF181 模式下数据含义不变。
- **CMD_SPI 模式下**，`0x680 + NodeID` 不再承载截断后的力/力矩数据：`force_raw` 和 `data_valid` **固定为 0**；完整力矩数据改由 `0x6B0 + NodeID` 发送。
- 原配置字 bit0~17（周期 + 总线格式）定义保持不变。
- 旧版配置未设置新增高位时：模式默认为**兼容模式**，力/力矩模块默认为 **DF181**。
- 若只需原有报文，将模式字段（bit18~19）设为 0 即可。

---

## 2 配置方法

透传配置使用 CANopen 对象 `0x5503` 子索引 `0x04`，数据类型 uint32，运行时写入后生效。

### 2.1 配置字定义（0x5503:04, uint32）

| 位 | 字段 | 取值 | 说明 |
|----|------|------|------|
| bit0~15 | period_div | 0~65535 | 发送周期分频值，0=关闭透传 |
| bit16~17 | bus_format | 0/1/2/3 | CAN 总线格式选择 |
| bit18~19 | mode | 0/1/2/3 | 透传报文模式选择 |
| bit20~21 | force_module | 0/1/2/3 | 力/力矩传感器模块选择 |
| bit22~31 | 保留 | 0 | 客户配置应保持为 0 |

### 2.2 SDO 写入格式（expedited download 4 字节）

| 字段 | 值 |
|------|----|
| 请求 COB-ID | 0x600 + NodeID |
| 响应 COB-ID | 0x580 + NodeID |
| DLC | 8 |
| 完整 8 字节 | `23 03 55 04 PL PH CFG 00` |

| 字节 | 内容 | 说明 |
|------|------|------|
| Byte0 | 0x23 | expedited download 4 bytes |
| Byte1~2 | 03 55 | 对象索引 0x5503，低字节在前 |
| Byte3 | 04 | 子索引 0x04 |
| Byte4 | PL | period_div 低 8 位 |
| Byte5 | PH | period_div 高 8 位 |
| Byte6 | CFG | `bus_format \| (mode << 2) \| (force_module << 4)` |
| Byte7 | 00 | 保留，固定 0 |

### 2.3 发送周期（基准 1 count = 0.5 ms）

- 发送周期(ms) = `period_div × 0.5`
- 发送频率(Hz) = `2000 / period_div`

| period_div | 周期 | 频率 |
|------------|------|------|
| 0 | 关闭 | 0 Hz |
| 4 | 2 ms | 500 Hz |
| 20 | 10 ms | 100 Hz |
| 40 | 20 ms | 50 Hz |
| 400 | 200 ms | 5 Hz |

### 2.4 CAN 总线格式

| 值 | 格式 | 说明 |
|----|------|------|
| 0 | Classical CAN | 经典 CAN，8 字节数据 |
| 1 | Classical CAN | 当前按经典 CAN 处理，建议用值 0 |
| 2 | CAN FD No BRS | CAN FD，不启用速率切换 |
| 3 | CAN FD BRS | CAN FD，启用速率切换 |

所有透传帧均使用标准 11 位 CAN ID，DLC 固定为 8。

### 2.5 透传模式

| 值 | 模式 | 发送报文 |
|----|------|----------|
| 0 | 兼容模式 | `0x680 + NodeID` |
| 1 | 运行反馈模式 | `0x690`、`0x6A0`、`0x6B0` + NodeID |
| 2 | 全部模式 | 兼容帧 + 运行反馈帧，共 4 帧 |
| 3 | 保留 | 当前版本不发送报文，不建议使用 |

### 2.6 力/力矩传感器模块

| 值 | 模块 | 通道说明 |
|----|------|----------|
| 0 | DF181 | 双通道，CH0 和 CH1 均可用 |
| 1 | CMD_SPI | 单通道，仅 CH0 有效；原始值为 **24 位有符号数** |
| 2/3 | 保留 | 当前按 DF181 处理，客户不应使用 |

> CMD_SPI 采用 MISO Ready 上升沿触发采样。切换为 DF181 后 CMD_SPI 专用 Ready 中断自动关闭；再切回 CMD_SPI 自动开启。
> V1.1 对每次 CMD_SPI 数据读取执行 CRC-8/MAXIM 校验，校验失败时本次样本无效。

### 2.7 CMD_SPI 数据与 CRC 校验（V1.1 新增）

CMD_SPI 每次读取返回 **3 字节原始数据 + 1 字节 CRC**。3 字节按传感器返回顺序组成 24 位二进制补码有符号数：

```
raw_s24 = sign_extend((data0 << 16) | (data1 << 8) | data2)
```

CRC 使用 **CRC-8/MAXIM** 算法：
- 初始值 `0x00`
- 反向多项式 `0x8C`（对应标准多项式 `0x31`）
- 计算字节顺序：`data2, data1, data0`

校验结果与返回 CRC 字节不一致时：
- 本次 CMD_SPI 样本标记为**无效**；
- 错误码记录为 **10（CRC 错误）**；
- `0x6B0 + NodeID` 的 `valid.bit0` 清零，并在 `error` 字段报告错误码。

---

## 3 CAN ID 与发送顺序

| CAN ID | 名称 | 内容 |
|--------|------|------|
| `0x680 + NodeID` | 兼容外部传感器帧 | Hall、膝关节 ADC、开关量；DF181 模式下还含 CH0 力矩与有效状态 |
| `0x690 + NodeID` | 运行反馈帧 0 | 实际位置、实际速度 |
| `0x6A0 + NodeID` | 运行反馈帧 1 | Iq 电流、母线电流、电机温度、错误码 |
| `0x6B0 + NodeID` | 力/力矩反馈帧 | DF181 双通道 S16，或 CMD_SPI 单通道 S24 + 有效位 + 错误码 |

- NodeID 使用设备模块 ID，有效范围 1~127。
- 全部模式发送顺序固定：`0x680 → 0x690 → 0x6A0 → 0x6B0`
- 运行反馈模式发送顺序固定：`0x690 → 0x6A0 → 0x6B0`
- CAN 发送邮箱暂不足时，固件保留未发送帧并在后续主循环继续发送，**不会因一次邮箱不足而丢弃整组报文**。

---

## 4 报文定义

> 除兼容帧（0x680）为连续 bit 位打包外，其余多字节字段均为**小端**（低字节在前）。

### 4.1 兼容外部传感器帧 — `0x680 + NodeID`

长度 8 字节，64 位小端 bit 位流。

| bit | 字段 | 类型 | 范围 | 说明 |
|-----|------|------|------|------|
| 0~11 | hall_adc0 | U12 | 0~4095 | Hall ADC 通道 0 |
| 12~23 | hall_adc1 | U12 | 0~4095 | Hall ADC 通道 1 |
| 24~35 | hall_adc2 | U12 | 0~4095 | Hall ADC 通道 2 |
| 36~49 | force_raw | U14 | 0~16383 | DF181 CH0 原始值；**CMD_SPI 模式固定为 0** |
| 50~61 | knee_adc | U12 | 0~4095 | 膝关节 ADC |
| 62 | hw_switch | U1 | 0/1 | PC9 硬件开关电平 |
| 63 | data_valid | U1 | 0/1 | DF181 CH0 有效标志；**CMD_SPI 模式固定为 0** |

接收端组合：`payload = data[0] | (data[1]<<8) | ... | (data[7]<<56)`

### 4.2 运行反馈帧 0 — `0x690 + NodeID`

| 字节 | 字段 | 类型 | 单位/说明 |
|------|------|------|-----------|
| 0~3 | position_actual | S32 LE | 实际位置，与 CANopen 0x6064 一致 |
| 4~7 | velocity_actual | S32 LE | 电机侧实际速度，rpm，与 0x606C 一致 |

### 4.3 运行反馈帧 1 — `0x6A0 + NodeID`

| 字节 | 字段 | 类型 | 单位/说明 |
|------|------|------|-----------|
| 0~1 | iq_current | S16 LE | Iq 实际电流，与 CANopen 0x6078 一致 |
| 2~3 | bus_current | S16 LE | 母线电流，mA；超范围饱和 |
| 4~5 | motor_temp | S16 LE | 电机线圈温度，0.1°C；超范围饱和 |
| 6~7 | error_code | U16 LE | 当前错误码，与 CANopen 0x603F 一致 |

### 4.4 力/力矩反馈帧 — `0x6B0 + NodeID`

**帧格式随 force_module 配置切换**，DLC 均为 8。

#### 4.4.1 DF181 模式（force_module=0）

| 字节 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 0~1 | force_ch0 | S16 LE | CH0 的 14 位有符号原始值，经符号扩展 |
| 2~3 | force_ch1 | S16 LE | CH1 的 14 位有符号原始值，经符号扩展 |
| 4 | valid_mask | U8 | bit0=CH0 有效，bit1=CH1 有效 |
| 5~7 | 保留 | U24 | 固定为 0 |

#### 4.4.2 CMD_SPI 模式（force_module=1）

| 字节 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 0~2 | force_ch0 | S24 LE | CH0 的 24 位有符号原始值，二进制补码，小端 |
| 3 | 保留 | U8 | 固定为 0 |
| 4 | valid | U8 | bit0=CH0 有效；bit1~7 固定为 0 |
| 5 | error | U8 | CH0 最近错误码：0=无错误，10=CRC 错误 |
| 6~7 | 保留 | U16 | 固定为 0 |

> ⚠️ CMD_SPI 的 24 位原始值**不能再按旧版 S16 解析**。接收端应先按小端组合 24 位无符号数，再依 bit23 执行符号扩展。
> CMD_SPI 单通道时 CH1 无数据（旧 DF181 帧的 CH1 概念不适用）。

---

## 5 配置示例（NodeID=1，period_div=4 即 2 ms，经典 CAN）

| 需求 | 完整 SDO 请求数据 | CFG 字节说明 |
|------|-------------------|--------------|
| 关闭透传 | `23 03 55 04 00 00 00 00` | period_div=0 |
| DF181，兼容模式 | `23 03 55 04 04 00 00 00` | CFG=0x00 |
| DF181，运行反馈模式 | `23 03 55 04 04 00 04 00` | CFG=0x04 (mode=1) |
| DF181，全部模式 | `23 03 55 04 04 00 08 00` | CFG=0x08 (mode=2) |
| CMD_SPI，兼容模式 | `23 03 55 04 04 00 10 00` | CFG=0x10 (force=1) |
| CMD_SPI，运行反馈模式 | `23 03 55 04 04 00 14 00` | CFG=0x14 (mode=1,force=1) |
| CMD_SPI，全部模式 | `23 03 55 04 04 00 18 00` | CFG=0x18 (mode=2,force=1) |

> CAN FD：将 CFG 的 bit0~1（bus_format）置 2 或 3。
> 例：CAN FD BRS + CMD_SPI + 全部模式 = `bus(3) | mode(2)<<2 | force(1)<<4` = `3 | 8 | 16` = **0x1B** → `23 03 55 04 04 00 1B 00`
> 字段位置速记：CFG 字节 bit0~1=bus_format，bit2~3=mode，bit4~5=force_module。

---

## 6 客户升级注意事项

1. 旧版上位机保持模式 0，继续解析 `0x680 + NodeID` 即可。
2. 新版上位机应按模式订阅对应 CAN ID，**不应假设每周期只有一帧**。
3. 全部模式每周期最多 4 帧，配置高发送频率前评估 CAN 总线负载。
4. 力/力矩物理换算系数不在本协议定义，本协议传原始计数值。**CMD_SPI 接收端必须按 S24 解析，不得截断为 S16**。
5. `0x680 + NodeID` 与应用 Boot 升级接收 ID 存在复用约束，启用透传时**勿同时执行应用升级**。
6. 保留字段必须写 0，避免后续固件扩展兼容性问题。

---

## 7 验收建议

- 写模式 0，确认只收到 `0x680`。
- 写模式 1，确认依次收到 `0x690、0x6A0、0x6B0` 三帧。
- 写模式 2，确认每周期最多 4 帧且 ID 顺序正确。
- 切换 DF181/CMD_SPI，确认 `0x6B0` 分别符合双通道 S16 与单通道 S24 格式。
- CMD_SPI 模式注入 CRC 错误，确认 `0x6B0` 的 `valid.bit0=0` 且 `error=10`。
- 分别选经典 CAN / CAN FD No BRS / CAN FD BRS，确认总线格式正确。
- 修改 `period_div`，确认发送周期满足 `period_div × 0.5 ms`。

---

## 附录 A：CRC-8/MAXIM 参考

| 参数 | 值 |
|------|----|
| 多项式（标准）| 0x31 (x⁸+x⁵+x⁴+1) |
| 多项式（反向）| 0x8C |
| 初始值 | 0x00 |
| 输入反射 | 是（RefIn=true） |
| 输出反射 | 是（RefOut=true） |
| 输出异或 | 0x00 |
| 计算字节序 | data2, data1, data0 |

```c
/* CMD_SPI 24位 + CRC-8/MAXIM 解析参考 */
uint8_t crc8_maxim(const uint8_t *d, int n) {
    uint8_t crc = 0x00;
    for (int i = 0; i < n; i++) {
        crc ^= d[i];
        for (int b = 0; b < 8; b++)
            crc = (crc & 1) ? (crc >> 1) ^ 0x8C : (crc >> 1);
    }
    return crc;
}

/* 校验: 顺序 data2,data1,data0 */
uint8_t buf3[3] = { data2, data1, data0 };
uint8_t crc = crc8_maxim(buf3, 3);
int valid = (crc == crc_rx);

/* 24位有符号还原 */
int32_t raw = (int32_t)((data0 << 16) | (data1 << 8) | data2);
if (raw & 0x800000) raw |= 0xFF000000;  /* bit23 符号扩展 */
```

---

## 整理说明（相对 V1.1 原文的修正）

V1.1 原文有 3 处 `0x680` 疑似应为 `0x6B0`，本整合版已按上下文修正，**待阳志强最终确认**：
1. 版本升级点："调整 CMD_SPI 模式下 0x680 与 ~~0x680~~ → **0x6B0**"
2. 兼容性结论："完整数据改由 ~~0x680~~ → **0x6B0** 发送"
3. 2.6 校验："~~0x680~~ → **0x6B0** 的 valid_bit 清零、error 字段报错"（因 valid/error 字段位于 0x6B0 的 byte4/byte5，0x680 兼容帧无 error 字段）

另：V1.1 原文 2.1 表仅列出 period_div 一行，本版已补回 V1.0 的 bus_format/mode/force_module/保留 完整定义。
