# 透传协议 V1.1 落地 自审报告

> 工程: joint-motor-hal-c-v3
> 实现: 张君宝 (张三丰)
> 日期: 2026-07-06
> 依据: 透传V1.1改造_实施规格.md + KWS外部传感器透传协议_整合版_V1.1.md + Claw 架构审查纠偏
> 状态: 已实现 + host 语法自检通过, 未 commit

---

## 一、交付概览

- 改动 13 文件, +181 / -10, 全部为新增 / append-only, 无破坏性修改。
- host 语法检查全绿:
  - motor_hal (gcc -std=c11 -D_GNU_SOURCE -fsyntax-only): canopen_frames.c / motor_hal.c → exit 0
  - stark_periph_node (g++ -std=c++17 -fsyntax-only): motor_rt_worker.cpp / motor_init.cpp / main_loop.cpp / main.cpp → exit 0
  - stark_client.h 以 C 和 C++ 两种方式包含 → exit 0
  - stark_shm_t: `_Static_assert(sizeof <= 64KB)` 通过
- 未 git commit (按要求等架构审查)。

改动文件:
```
motor_hal/inc/canopen_frames.h      motor_hal/inc/motor_hal.h
motor_hal/inc/motor_hal_types.h     motor_hal/src/canopen_frames.c
motor_hal/src/motor_hal.c           stark_periph_node/src/main.cpp
.../main_loop/main_loop.cpp         .../motor/motor_context.h
.../motor/motor_init.cpp            .../motor/motor_init.h
.../motor/motor_rt_worker.cpp       .../stark_client.h
.../stark_shm.h
```

---

## 二、6 条硬约束逐条核对

| # | 约束 | 落实 | 结论 |
|---|------|------|------|
| 1 | 默认 CMD_SPI (force_module=1) | motor_hal_sensor_config 保持旧签名, 内部默认 FORCE_MODULE_SPI + SENSOR_MODE_ALL 调 _ex | 通过 |
| 2 | 周期最快 (period_div=1=0.5ms) | 节点 sensor_period_div 默认 1, main_loop 直接下发; 校准 motor_calib.c 也改为 period_div=1 (原硬编码 4=2ms 会覆盖, 已修) | 通过 |
| 3 | 上层接口 append-only | stark_client.h 仅新增 stark_spi_force; SHM 仅在 stark_shm_t 绝对末尾追加; motor_sensor_t 尾部追加 | 通过 |
| 4 | 基于 V1.1 文档 | 帧格式 4.4.2 / 配置字 2.1 / S24LE / 无 CRC 均按 KWS V1.1 | 通过 |
| 5 | 改完自审 | 本报告 | 通过 |
| 6 | 不影响其他功能 | 见第四节 | 通过 |

---

## 三、4 个设计决策落实

- 决策1 (mode 必须=2): `SENSOR_MODE_ALL=2`, sensor_config 默认下发 mode=2, 保证 0x680 与 0x6B0 同时发送。
- 决策2 (CMD_SPI 下 0x680 force_raw/data_valid 固定 0, 力矩只在 0x6B0):
  - 0x680 解析路径 (_parse_sensor_frame) 保持不变, CMD_SPI 下设备把 force_raw/data_valid 置 0, 主站如实读到 0。
  - 力矩新源为 spi_force_raw_s24 (0x6B0), 通过 stark_spi_force 提供。算法侧改用该接口取力矩, 不再用 0x680 force_raw。
- 决策3 (尾部追加): motor_sensor_t 在 timestamp_us 之后追加; stark_shm_t 在末尾 _pad 切出追加。均真尾部。
- 决策4 (0.5ms 基准 + period_div 直接配默认 1): 节点 sensor_period_div 默认 1; config 优先读 period_div, 兼容旧 period_ms (×2 换算)。

---

## 四、"不影响其他功能" 的保证方式 (重点)

| 功能 | 保证方式 |
|------|---------|
| 0x300 反馈 | canopen_parse_feedback 及 0x300 dispatch 分支未触碰。0x6B0 拦截放在 switch 前, 用 0x7F0 精确掩码只匹配 0x6Bx, 不影响 0x680/0x690/0x6A0/0x300/SDO |
| SDO 遥测 | _sdo_telemetry_thread 未改 |
| mailbox 控制 | ProcessMailbox / mailbox 结构未改 |
| IMU | imu 路径未改 |
| 校准 | 校准状态机未改 |
| 上报 | PeriodicUploadData / periodic_data 本次不加字段 (按 Claw 决定留 phase-2); PublishFeedback 仅新增 spi seqlock 发布块, 现有 d 组装逻辑不动 |
| 二进制兼容 (append-only) | stark_shm_t 新字段在绝对末尾 (从 _pad 切出), 现有全部字段偏移零变化; static_assert 验证 size <= 64KB。旧算法二进制不重编仍读到正确现有字段 |
| 0x680 现有布局 | stark_sensor_data_t (feedback_frame_t 中间数组) 与 motor_sensor_t 现有字段一律未动, 仅尾部追加 |

关键设计点:
- **0x680/0x690/0x6A0/0x6B0 在 CANopen func_code (id & 0x780) 下同码**, 故 0x6B0 必须在 dispatch switch 前用 0x7F0 掩码单独拦截 (node<16 成立, STARK_MAX_MOTORS=2 满足), 否则会落入 0x680 分支被丢弃。
- SHM 新字段脱离双 Buffer, 采用独立 spi_version seqlock (奇=写入中 / 偶=完成), 读方前后对比检测撕裂, best-effort 最新值。

---

## 五、待设备验证 / 待确认项

1. **配置字 SDO 生效**: 默认配置字 = period_div(1) | bus_format(3<<16) | mode(2<<18) | force_module(1<<20) = **0x1B0001**, SDO 帧 `23 03 55 04 01 00 1B 00`。待设备实测确认写入 0x5503:04 后 0x6B0 正常发送。
2. **0x6B0 帧字段偏移**: 按协议 4.4.2 byte0~2=force_ch0 S24LE / byte4=valid / byte5=error 实现, 待设备实测确认字节序 (S24LE) 与 valid/error 位置。
3. **force_raw_s24 → 物理力矩换算**: 换算系数不在协议内, 本次 SHM 存原始 S24 计数 (spi_force_raw_s24), 未做 mNm 换算。
4. **0x690 (位置) / 0x6A0 (其他) 解析**: 本次未实现 (phase-2)。mode=2 下会收到, 落 dispatch default 分支无害忽略, 不影响现有功能。
5. **CRC**: 按 Claw 纠正, 主站不做任何 CRC 校验 (0x6B0 帧无 CRC 字节, 设备侧 CRC-8/MAXIM 已体现在 valid/error), 相关代码已删除。
6. **OD index**: 保持 0x5503:04 (阳志强确认, 非 0x2670)。

---

## 六、验收清单

- [x] F1~F8 全部实现
- [x] host g++ / gcc -fsyntax-only 通过 (含 stark_client.h C/C++)
- [x] 6 条约束逐条核对
- [x] 4 个设计决策落实
- [x] 0x6B0 无 CRC (已删) + S24LE 力矩 + valid/error
- [x] 不影响 0x300 / SDO / mailbox / IMU / 校准 / 上报
- [x] append-only (SHM 绝对末尾追加 + sizeof <= 64KB 校验)
- [x] 终审收尾: 校准 period_div=1 (约束2) + seqlock ARM RELEASE fence
- [x] 未 commit, 待架构审查

---

## 七、终审收尾修改 (Claw 终审后)

1. **必修 (约束2)**: motor_hal/src/motor_calib.c Step5 校准结束的传感器透传配置, 由硬编码 period_div=4 (V1.1 下=2ms, 会绕过 main_loop 门控覆盖成慢周期) 改为 period_div=1 (0.5ms/2000Hz), 与 main_loop 默认最快周期一致, 满足约束2。tools/cmd_sensor.c、test/hal_stress_test.c 的调用为工具/测试, 按 Claw 意见不动。
2. **建议修 (ARM 弱内存加固)**: motor_rt_worker.cpp spi seqlock 发布, 在 store(sv+1, odd, RELEASE) 后、写数据前增加 __atomic_thread_fence(__ATOMIC_RELEASE), 防止普通数据 store 在 RK357x 弱内存序下被重排到 odd marker 之前。

两处均 host 语法复检通过 (gcc/g++ exit=0), 未 commit。

---

## 附: F1~F8 实现位置

| 项 | 文件 | 内容 |
|----|------|------|
| F1 | motor_hal_types.h | COB_SPI_TORQUE_BASE=0x6B0; force_module_t; 配置字位定义宏; motor_sensor_t 尾部追加 spi_force_raw_s24/spi_valid/spi_error/spi_timestamp_us |
| F2 | motor_hal_types.h | spi_force_frame_t (force_raw_s24/valid/error) |
| F3 | canopen_frames.c/.h | canopen_parse_spi_force (S24LE + valid + error, 无 CRC) |
| F4.1 | motor_hal.c | _dispatch_frame switch 前 0x7F0 拦截 0x6B0 + 加锁缓存 |
| F4.2/F5 | motor_hal.c/.h + 节点 | motor_hal_sensor_config_ex(period_div,bus_format,mode,force_module); 旧接口默认 SPI+mode2; 节点 period_div 直接配默认 1 |
| F6 | stark_shm.h | stark_shm_t 末尾追加 spi_force_raw_s24[]/spi_valid[]/spi_error[]/spi_version, 缩小 _pad |
| F7 | motor_rt_worker.cpp | PublishFeedback 收集 0x6B0 + seqlock 发布到 SHM 末尾 |
| F8 | stark_client.h | stark_spi_force (seqlock 读) |

---

## 八、PeriodicUploadData 重构 + 命名/注释整理 (最终版)

### 8.1 力矩数据路径重构 (单一 report 接口)
将 0x6B0 力矩从“stark_shm_t 末尾独立 seqlock 块”迁到 PeriodicUploadData 内, 单一数据路径 + 单一接口:
- stark_shm.h: PeriodicUploadData 尾部追加 spi_force_raw_s24/spi_valid/spi_error (右) + _left (左); 紧跟的 _pad_rpt 由 32 缩至 16 吸收新增 16B。
- 删除旧方案: stark_shm_t 末尾的 spi_force_raw_s24[]/spi_valid[]/spi_error[]/spi_version 及 _pad 恢复至 3130; motor_rt_worker.cpp 独立 seqlock 发布块; stark_client.h 的 stark_spi_force()。
- motor_rt_worker.cpp: 在组装 PeriodicUploadData 时填 d.spi_force_raw_s24 / spi_valid / spi_error (右 id==1) 与 _left (左 id==2), 由 periodic_version 原子保护。
- demo_algo.c: SPI 行改为直接读 d->spi_force_raw_s24 / _left。

### 8.2 knee 命名修正
knee_adc / knee_angle 实为膝关节霍尔, 全工程改名: motor_sensor_t.knee_adc → knee_hall; stark_sensor_data_t.knee_adc → knee_hall; PeriodicUploadData.knee_angle/_left → knee_hall/_left; 赋值/打印/工具(tool_hal.c/cmd_report.c/cmd_sensor.c/hal_stress_test.c)/WebServer.cpp 同步。

### 8.3 注释整理
本次改动注释已改写为正常在职工程注释, 描述代码行为; 无外部文档/版本/协作性引用。

### 8.4 append-only 验证 (offsetof 实测, 重构版 vs 原始 HEAD)

| 项 | 原始 HEAD | 重构版 | 结论 |
|----|----------|--------|------|
| sizeof(stark_shm_t) | 4608 | 4608 | 不变, <= 64KB |
| sizeof(PeriodicUploadData) | 140 | 156 | +16 (符合预期) |
| offsetof(periodic_data) | 1244 | 1244 | 不变 |
| offsetof(mgmt_cmd) | 1416 | 1416 | 不变 |
| offsetof(sdo_cmds) | 1424 | 1424 | 不变 |
| offsetof(algo_heartbeat) | 1460 | 1460 | 不变 |
| offsetof(rt_cycle) | 1468 | 1468 | 不变 |

PUD +16 由 _pad_rpt -16 吸收, 后续字段偏移零变化, 总 size 不变。

### 8.5 交付
- host 语法全绿 (motor_hal C / 节点 C++ / stark_client.h C及C++ / demo_algo.c / WebServer.cpp / 工具文件)
- 完整 patch: 透传V1.1_完整.patch (19 文件, +169/-30, 不含 .md, 含 demo_algo)
- .md 文档不进 patch / 不 commit
- 未 commit
